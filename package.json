{
  "name": "whatsapp-webjs-server",
  "version": "1.0.0",
  "description": "REST API server built on top of whatsapp-web.js. Supports up to 5 concurrent sessions, exposes CRUDL endpoints for session management and allows interacting with chats, messages, media and contacts via HTTP. Incoming messages can be forwarded to a configurable webhook per‑session, including downloaded media where possible.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "author": "Your Name",
  "license": "Apache-2.0",
  "dependencies": {
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mime-types": "^2.1.35",
    "multer": "^1.4.5-lts.1",
    "swagger-ui-express": "^4.6.3",
    "whatsapp-web.js": "^1.34.1",
    "yamljs": "^0.3.0"
  }
}